(()=>{'use strict';const {S,save}=window.MAKER03;document.querySelector('#design-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),f=document.querySelector('#design-feedback');if(![d.get('wheel-pair'),d.get('fair-test'),d.get('safety')].every(Boolean))return void(f.textContent='ตอบให้ครบทั้ง 3 ส่วน / Complete all parts.');if(d.get('wheel-pair')==='matched'&&d.get('fair-test')==='one'&&d.get('safety')==='safe'){f.textContent='แผนดี: ล้อคู่สมดุล เปลี่ยนทีละตัวแปร และทดสอบบนพื้นราบ / Good plan.';S.designComplete=true;save()}else f.textContent='ทบทวนล้อคู่ การทดสอบทีละตัวแปร และความปลอดภัย / Review the plan.'});document.querySelector('#quiz-form').addEventListener('submit',e=>{e.preventDefault();const d=new FormData(e.currentTarget),f=document.querySelector('#quiz-feedback');if(![d.get('q1'),d.get('q2'),d.get('q3')].every(Boolean))return void(f.textContent='ตอบให้ครบทุกข้อ / Answer every question.');const n=+(d.get('q1')==='together')+(d.get('q2')==='friction')+(d.get('q3')==='evidence');if(n===3){f.textContent='ครบถ้วน 3/3 / Complete 3/3.';S.quizComplete=true;save()}else f.textContent=`${n}/3 — ทบทวนโครงสร้าง ข้อจำกัด และหลักฐาน / Review structure, limits and evidence.`});})();