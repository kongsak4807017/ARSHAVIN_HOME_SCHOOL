import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
const lesson=read('subjects/maker-engineering/friction-surfaces-safer-motion.html');
const script=read('assets/js/friction-surfaces-lesson.js');
const worksheet=read('worksheets/student/friction-surfaces-safer-motion-a4.html');
const guide=read('worksheets/teacher-guides/friction-surfaces-safer-motion-guide.html');
const shell=read('assets/js/learning-shell.js');
const sw=read('service-worker.js');
assert.match(lesson,/MAKER-06/);
assert.match(lesson,/แรงเสียดทาน|friction/i);
assert.match(lesson,/fair test/i);
assert.match(lesson,/ไม่แข่งความเร็ว|no speed/i);
assert.match(lesson,/ทางลาดสูง|high ramps/i);
assert.match(lesson,/aria-live="polite"/);
assert.match(lesson,/fieldset/);
assert.match(script,/arshavin\.maker\.friction\.v1/);
assert.match(script,/comparisonComplete/);
assert.match(script,/designComplete/);
assert.match(script,/quizComplete/);
assert.doesNotMatch(script,/fetch\(|XMLHttpRequest|WebSocket|sendBeacon/);
assert.equal((worksheet.match(/class="worksheet page"/g)||[]).length,2);
assert.match(worksheet,/@page\{size:A4/);
assert.match(guide,/Rubric 4 ระดับ/);
assert.match(guide,/ไม่มี timer|ไม่มีการแข่งขัน/);
assert.match(shell,/MAKER-06/);
assert.match(shell,/arshavin\.maker\.friction\.v1/);
for(const path of ['friction-surfaces-safer-motion.html','friction-surfaces-lesson.js','friction-surfaces-safer-motion-a4.html','friction-surfaces-safer-motion-guide.html'])assert.ok(sw.includes(path),`offline cache missing ${path}`);
console.log('MAKER-06 focused static checks: PASS');