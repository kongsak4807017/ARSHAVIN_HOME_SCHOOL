import fs from 'node:fs';
import assert from 'node:assert/strict';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
const lesson=read('subjects/human-body/excretion-kidneys-water-balance.html');
const script=read('assets/js/kidneys-water-balance-lesson.js');
const worksheet=read('worksheets/student/excretion-kidneys-water-balance-a4.html');
const guide=read('worksheets/teacher-guides/excretion-kidneys-water-balance-guide.html');
for(const text of [lesson,worksheet,guide]) assert.match(text,/ไต|kidney/i);
assert.match(lesson,/Kidneys[\s\S]*Ureters[\s\S]*Bladder[\s\S]*Urethra/i);
assert.match(lesson,/แบบจำลองมีขอบเขต|Model limits/i);
assert.match(lesson,/ไม่วินิจฉัย|does not diagnose/i);
assert.match(lesson,/ไม่ต้องบอก|privacy/i);
assert.match(lesson,/แข่งขันดื่มน้ำ|water-drinking challenges/i);
assert.match(lesson,/fieldset/); assert.match(lesson,/legend/); assert.match(lesson,/aria-live="polite"/);
assert.doesNotMatch(script,/fetch\s*\(|XMLHttpRequest|WebSocket|sendBeacon/);
assert.match(script,/arshavin\.humanbody\.kidneys\.v1/);
assert.match(script,/pathComplete/); assert.match(script,/balanceComplete/); assert.match(script,/quizComplete/);
assert.equal((worksheet.match(/class="worksheet(?:\s|")/g)||[]).length,2);
assert.match(worksheet,/@page\{size:A4/);
assert.match(guide,/Rubric 4 ระดับ/);
assert.match(guide,/ปัสสาวะเป็นเลือด|urine/i);
console.log('HB-07 focused static checks passed.');